# frontend/src/modules

这里按左侧菜单组织代码。你看到什么菜单，就来找同名目录。

每个菜单目录固定分两类：

```text
接口测试/
  接口测试功能/   放功能实现入口、接口调用说明、校验逻辑、代码侧自动化入口说明
  接口测试UI/     放页面、表格、表单、按钮、弹窗等 UI 代码
```

改造原则：

- 改页面长什么样：进对应的 `xxxUI`。
- 改功能怎么跑：先看对应的 `xxx功能/README.md`，再去后端或 worker 的真实实现。
- 通用工具仍放在 `frontend/src/shared/`，不要在每个模块里重复复制。
