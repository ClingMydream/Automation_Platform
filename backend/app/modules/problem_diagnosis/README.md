# 问题定位模块

本模块负责把失败结果转成可跟踪的问题定位记录，承接平台主链路中的“结果采集 -> 问题定位 -> 质量分析”。

- `router.py`：对外提供问题定位 CRUD，以及从失败结果自动生成定位记录的接口。
- `schemas.py`：定义请求和响应结构，字段会同步展示到 Swagger。
- `service.py`：集中放置关联校验、失败分类推断、严重级别推断和证据组装逻辑。

当前版本只做安全的证据整理和建议生成，不执行用户上传脚本，也不自动访问外部缺陷平台。
